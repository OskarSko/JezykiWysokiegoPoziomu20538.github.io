export default function ZainteresowaniaLayout({ children }) {
    return (
        <div style={{ fontFamily: "Tahoma, sans-serif", color: "darkred", textAlign: "center" }}>
            {children}
        </div>
    );
}
